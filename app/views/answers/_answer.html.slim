h3 
  | 質問
br
  = @question.title
br
  = @question.body 

- answers.each do |answer|
  - unless answer.nil?
    p
      = link_to "#{answer.user.name}さん", profile_path(current_user)
    p
      | コメント：
      = answer.content
    p 
    | <div id="like-
    = answer.id
    | ">
    = render partial: "likes/like", locals: { answer: answer }

    - if answer.user == current_user
      p
        = link_to '削除する', question_answer_path(answer.question_id, answer.id), method: :delete, remote: true
    p
    = form_with model: @answer, local: true do |f|
      | 返答：
      = f.text_field :content 
      br
      = f.submit "投稿"

